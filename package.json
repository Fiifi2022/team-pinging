{
  "name": "team-ping",
  "version": "1.0.0",
  "description": "Local network ping notification tool for teams",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
